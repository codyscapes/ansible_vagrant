- name: update apt-get 
  apt: update_cache=yes

- name: Install virtualenv 
  apt: name=python-virtualenv state=latest

- name: Installs pip
  apt: name=python-pip state=latest

- name: Load virtualenv into stenosaurus directory
  command: virtualenv /vagrant/stenosaurus/env

- name: Install pyramid and pyramid-make through virtualenv
  easy_install: name={{ item }} virtualenv=/vagrant/stenosaurus/env
  with_items:
    - bin/pyramid
    - bin/pyramid-mako

- name: install python-dev
  apt: name=python-dev

- name: Installs nginx
  apt: name=nginx state=latest

- name: Installs uwsgi
  command: pip install uwsgi